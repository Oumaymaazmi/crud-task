spring:
  application:
    name: task

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: tas-demo
    password: task
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

  flyway:
    out-of-order: true
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  security:
    oauth2:
      resourceserver:
        jwt:
          secret: "K8fY1r@vM#7n!pLzEwQsXcVjBzTkGhRm"
          expiration-time : 86400000
logging:
  level:
    org.flywaydb: DEBUG

app:
  front-end-url: http://localhost:3000/